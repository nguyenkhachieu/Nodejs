extends ../layouts/common

block content

    h1 View user
    p Name
        if user
            b=user.name
    p Phone
        if user
            b=user.phone
    p Avatar
        if user
            b
            img(src=user.avatar, width= "48")